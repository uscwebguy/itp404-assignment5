<td>
		{{#if isPopular}}
			{{fa-icon "star" size=1}}
		{{/if}}
	</td>
<td>
		{{#if isAffordable}}
			$
		{{else}}
			$$
		{{/if}}
	</td>
	
	<td>{{song.title}}</td>
	<td>{{song.price}}</td>
	<td>{{song.playCount}}</td>
	<td><a class="{{if isLiked 'liked'}}" href="#" onClick={{action "like"}} ><i class="fa fa-thumbs-up" aria-hidden="true"></i></a></td>
	
	